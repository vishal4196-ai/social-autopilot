[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m src.main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

# Mount a persistent volume so SQLite survives redeploys.
# Add the volume in Railway dashboard with mount path = /data
